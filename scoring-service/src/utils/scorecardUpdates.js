const playerStatsUpdate = (scorecard, playerId, statType, statValue) => {
    

    // if (!playerStats.has(playerId)) {
    //     playerStats.set(playerId, new Map());
    // }
    // const playerStat = playerStats.get(playerId);
    // if (!playerStat.has(statType)) {
    //     playerStat.set(statType, 0);
    // }
    // playerStat.set(statType, playerStat.get(statType) + statValue);
}

module.exports = {
    playerStatsUpdate
}